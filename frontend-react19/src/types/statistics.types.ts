export type statisticsType = {
    total_users: string;
    total_cars: string;
    total_new_users: string;
    total_new_cars: string;
};

export type statisticsBarChart = {
    date: string;
    users: string;
    cars: string;
};

export type statisticsPieChartGender = {
    male_data: string;
    female_data: string;
};

export type statisticsPieChartBrand = {
    brand: string;
    count: string;
};
